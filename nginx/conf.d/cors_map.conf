# CORS 허용 Origin 매핑 (http 블록에서 로드됨)
# conf.d/include 순서상 이 파일이 default.conf보다 먼저 로드되어야 함
map $http_origin $cors_origin {
    default "";
    "~^https://(www\.)?gmss\.site$" $http_origin;
    "https://testchat-alpha.vercel.app" $http_origin;
}
